import { writable, derived } from "svelte/store";

/**
 * Use writable to handle people, and derived to handle isPeopleLoading base on people store
 */

export const people = null;

export const isPeopleLoading = false;
